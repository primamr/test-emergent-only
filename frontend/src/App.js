function App() {
  return (
    <div data-testid="hello-world-page" style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", minHeight: "100vh" }}>
      <h1 data-testid="hello-world-text">Hello World Claude</h1>
      <p data-testid="welcome-text">welcome to vibing era!</p>
      <p data-testid="nice-text">NICE!</p>
    </div>
  );
}

export default App;
